#repo type curently only suports Memory and BinaryFile
repository = Memory

#repo path to data files
#in case of Memory repo this is not used so leave it empty
persons=
activities= 
#in case of BinaryFile repo this is the path to the file
#in case of Memory repo this is not used so leave it empty